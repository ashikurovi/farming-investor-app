fix: Update ProjectCard to use img tag with correct photoUrl

- Replaced placeholder with actual img tag implementation
- Used project.images[0] directly as source (clean URL passed from parent)
- Added fallback image handling
- Added category and status badges back to the image container
